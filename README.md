To run the project:

```
// install dependencies and activate the poetry env
$ poetry install
$ poetry shell

// run the sandbox
$ sh sandbox up dev

// run the deploy script
$ poetry run python src/deploy.py
```
